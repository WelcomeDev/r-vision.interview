/** Настройки эмуляции сети. Меняйте, чтобы проверить состояния загрузки и ошибок. */
export const apiConfig = {
  /** Базовая задержка ответа, мс. */
  latencyMs: 500,
  /** Доля запросов, падающих с 500-й ошибкой (0..1). */
  failureRate: 0,
};
